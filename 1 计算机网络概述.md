## 1.计算机网络的形成（阿帕网->互联网）

ARPANET 于 1969 年由美国国防部 ARPA 资助建立，是世界上第一个分组交换网络，最初连接 UCLA、斯坦福研究院、加州大学圣巴巴拉分校和犹他大学四台计算机。ARPANET 采用分组交换技术替代传统的电路交换，奠定了互联网的技术基础。1983 年，ARPANET 全面从 NCP 迁移至 TCP/IP 协议族，这一事件被公认为互联网正式诞生的标志。此后 NSFNET 接替 ARPANET 成为骨干网，逐步发展为覆盖全球的互联网。

## 2.计算机网络的概念

> [!NOTE] 一句话概念
> 计算机网络是利用地理上分散的且具有独立功能的多个计算机系统，通过通信线路在相关软件的支持下实现的数据通信和资源共享的系统

![[Pasted image 20260727211326.png]]
## 3.两个计算机网络的例子
### 客户与服务端是如何建立连接的？
![[Pasted image 20260727213046.png]]


### 对等通信网络（p2p)

例如：迅雷
作用：加速下载速度
![[Pasted image 20260727213142.png]]


#### 如何做到？

P2P（对等通信）中，每个节点既是客户端也是服务器，消除了对单一中央服务器的依赖。以 BT 下载为例：通过 Tracker 或 DHT（分布式哈希表）发现拥有同一资源的其他节点；节点与多个对等方建立 TCP 连接，将文件拆分为多个数据片（piece），从不同节点并行下载不同数据片，同时向上传已拥有的数据片，实现"众人为我，我为众人"的去中心化传输。


## 计算机网络基本概念

### ISP

互联网服务提供商（Internet Service Provider），向用户提供互联网接入及相关服务的商业机构。ISP 通常分为三个层级：主干 ISP 拥有覆盖全国甚至跨国的光纤骨干网，为下级 ISP 提供互联；区域 ISP 在特定地理范围运营，连接主干与本地 ISP；本地 ISP 直接面向终端用户，通过光纤、DSL、移动网络等方式提供"最后一公里"接入。

### 节点

网络中具有独立网络地址、能收发数据的设备统称为节点。节点既可以是端系统（计算机、服务器、手机），也可以是中间系统（路由器、交换机）。每个节点在网络层拥有 IP 地址用于跨网通信，在数据链路层拥有 MAC 地址用于局域网内寻址。节点是构成网络拓扑的基本单元。

### 主机

连接在互联网上的端系统（end system），是网络通信的起点或终点。主机运行应用程序、产生或消费数据。按角色可分为服务器（常驻运行、被动等待请求）和客户机（主动发起通信）。在 P2P 架构中，一台主机可同时充当客户端和服务器。随着云计算与虚拟化的发展，虚拟机和容器也是常见的主机形态。

### 客户端

在客户/服务器（C/S）架构中，主动向服务器发起服务请求的一方称为客户端。客户端可以是运行在用户设备上的应用程序（如浏览器、微信），也可以是嵌入式终端设备。客户端通常不直接对外提供服务，其 IP 地址可能动态变化或经过 NAT 转换，通信由客户端发起，服务器被动响应后返回结果。

### 报文

报文（message）是应用层的数据传输单元，承载完整的通信内容，如一个 HTTP 请求/响应或一封电子邮件的正文。报文在向下传递时，经过传输层、网络层、数据链路层逐层添加头部（封装），最终以比特流形式发送至物理介质；接收端反向逐层解封装，还原出原始报文，每一层只关注本层头部所携带的控制信息。

### 分组

分组（packet）是网络层的数据传输单元。源主机的传输层将报文分段后交给网络层，网络层为每个段添加 IP 首部（含源/目的 IP 地址、TTL、协议版本、首部校验和等字段），封装为分组。每个分组独立路由，可能走不同路径到达目的地，IP 协议提供"尽力而为交付"服务，不保证分组不丢失、不乱序、不重复。

### 比特

比特（bit）是信息量的最小单位，取值只有 0 或 1。在物理层，数据最终转换为比特流（一串 0/1 序列）在线路上传输。物理层的核心任务就是确定如何用物理信号（电压高低、光脉冲、电磁波等）来表示比特 0 和 1，涉及编码、调制等关键技术。比特率（bps）是衡量信道传输能力的基本指标。

### 字节

8 比特组成 1 字节（Byte），是计算机存储和网络传输的基本计量单位。在计算机网络中，帧、分组、报文的大小通常以字节表示（如以太网帧最小 64 字节，IP 首部通常 20 字节，TCP 首部通常 20 字节）。注意带宽单位中的 B 通常指 Byte 而非 bit，1 Byte = 8 bit，描述速率时应分清区分 bps 与 Bps。

![[Pasted image 20260727213519.png]]

![[Pasted image 20260727214033.png]]

![[Pasted image 20260727214515.png]]

![[Pasted image 20260727214736.png]]

![[Pasted image 20260727214818.png]]


### 其他概念

#### 多线BGP
多线 BGP 指机房或企业同时接入多家运营商（如电信、联通、移动），并通过 BGP 协议对外通告自身 IP 地址段。BGP 可根据 AS 路径长度等策略选择最优路由，使得不同运营商的用户访问该服务器时，流量自动走对应运营商的线路，避免跨运营商访问的高延迟和丢包，同时实现链路冗余和负载均衡。
#### 网络穿透
由于 IPv4 地址枯竭，大量设备位于 NAT 或防火墙之后，没有独立公网 IP，无法被外网主动访问。网络穿透使内网设备可被公网访问，常见方法有：NAT 穿透（UDP 打洞、STUN/TURN 协议），使两个内网主机建立 P2P 直连；内网穿透工具（frp、ngrok），通过公网中转服务器将外部请求转发到内网服务，无需公网 IP 即可对外提供服务。


## 计算机网络的分类

### 分布范围

![[Pasted image 20260727214946.png]]

### 其他

![[Pasted image 20260727215111.png]]

### 交换方式分类
![[Pasted image 20260731014512.png]]
#### 电路交换
![[Pasted image 20260731014612.png]]
![[Pasted image 20260731014621.png]]
![[Pasted image 20260731015023.png]]
#### 报文交换
![[Pasted image 20260731015039.png]]
![[Pasted image 20260731104336.png]]
#### 分组交换
![[Pasted image 20260731104549.png]]
![[Pasted image 20260731104601.png]]
## OSI参考模型

### 概念：

OSI（Open System Interconnection）开放系统互连参考模型由国际标准化组织（ISO）于 1984 年提出，旨在为不同厂商的计算机系统互联提供统一标准。模型将网络通信自下而上划分为物理层、数据链路层、网络层、传输层、会话层、表示层、应用层共七层。每一层利用下层提供的服务，并为上层提供更高级的服务接口，层次间通过标准协议和接口解耦，实现"对等层通信"的抽象。

![[Pasted image 20260727215242.png]]

### 参考模型的交互


![[Pasted image 20260727215614.png]]

### 服务

层与层之间的
![[Pasted image 20260727215911.png]]

### OSI的问题
#### 错误的时机
OSI 标准制定过程极为缓慢，从 1970 年代末一直持续到 1984 年才发布最终标准。与此同时，TCP/IP 协议族早在 1970 年代就随 ARPANET 投入使用，1983 年被确立为正式协议并开始在 Unix 系统中广泛集成。当 OSI 标准最终完成时，TCP/IP 已在实际网络中大规模部署并形成了庞大的产业生态，技术和市场的路径依赖使得 OSI 难以被广泛采纳。
#### 糟糕的设计
OSI 七层模型在工程上存在明显缺陷：会话层（对话控制、同步点管理）和表示层（数据格式转换、加密解密）在实际实现中几乎总是合并到应用层，极少独立存在；部分层次功能边界模糊（如数据链路层与网络层的流控和差错控制职责重叠）；协议栈过于复杂、实现代价高，不符合互联网"简单务实"的设计哲学。

![[Pasted image 20260727220159.png]]

## TCP/IP参考模型

### 概念：

TCP/IP 协议族是当前互联网实际运行的标准协议体系，因核心协议 TCP 和 IP 得名，但并非仅含这两种协议。它采用四层架构：网络接口层（对应 OSI 的物理层和数据链路层）、网际层（核心为 IP 协议，负责寻址与路由）、传输层（TCP 提供可靠面向连接服务，UDP 提供不可靠无连接服务）、应用层（HTTP、DNS、FTP、SMTP 等面向应用的协议）。协议族遵循"沙漏模型"——IP 层承上启下，是全局统一的窄腰。

![[Pasted image 20260727220306.png]]


### 问题

TCP/IP 四层模型的主要不足：网络接口层将 OSI 的物理层和数据链路层合并为一层，定义过于宽泛，未对具体物理介质和链路协议做出规范，导致实现高度依赖具体硬件驱动；应用层承担了会话、表示、应用三层的全部功能，缺乏清晰的层次抽象，部分协议（如 TLS）的归属不明确；此外，模型未对安全机制进行层次化规划与统筹安排。

![[Pasted image 20260727220841.png]]

## 学习计算机网络真正的参考模型
### 五层参考模型


![[Pasted image 20260727221057.png]]

### 物理层

物理层是五层模型的最底层，负责在物理介质（双绞线、光纤、无线信道）上透明传输比特流。主要定义四大特性：机械特性（接口形状、引脚数，如 RJ-45）、电气特性（电压范围、传输速率）、功能特性（引脚含义，如发送/接收）、规程特性（建立连接的事件时序）。物理层不关心比特含义，只负责将 0/1 正确地在介质上表示与传输，涉及编码、调制等关键技术。

![[Pasted image 20260727221223.png]]


### 数据链路层

数据链路层在相邻节点之间实现以帧为单位的数据传输。主要功能包括：成帧（将比特流组装为帧，添加首部和尾部界定边界）、差错检测（通过 CRC 校验码发现传输错误，但不纠正）、流量控制（防止发送方速率过快淹没接收方）、介质访问控制（在广播信道中协调多节点共享介质，如 CSMA/CD 协议）。代表协议：以太网（IEEE 802.3）、点对点协议 PPP。

![[Pasted image 20260727224409.png]]

### 网络层

网络层负责将分组从源主机跨多个网络送达目的主机，核心功能为路由选择和分组转发。路由选择通过 OSPF、BGP 等路由协议构建路由表，确定最佳路径；分组转发由路由器根据路由表将分组从输入端口交换到输出端口。核心协议是 IP（互联网协议），提供无连接、尽力而为的交付服务。还包括 ICMP（差错报告与诊断，如 ping）、ARP（IP 地址到 MAC 地址映射）等辅助协议。

![[Pasted image 20260727231329.png]]

### 传输层

传输层为运行在不同主机上的应用进程提供端到端的逻辑通信服务，是唯一负责端到端通信的层次。提供两种服务模式：TCP——面向连接、可靠有序传输，含流量控制和拥塞控制，适用于文件传输、网页浏览等；UDP——无连接、不可靠、低开销，适用于视频直播、DNS 查询等实时场景。传输层通过端口号区分不同应用进程，实现复用与分用。

![[Pasted image 20260727231532.png]]

### 表示层

表示层负责处理网络中传输数据的语法和语义问题，确保不同系统间正确理解数据格式。主要功能：数据格式转换（如 ASCII 与 EBCDIC 编码互转、大小端字节序转换）、数据加密与解密（为应用数据提供机密性，实践中通常在应用层通过 TLS 实现）、数据压缩与解压缩（减少传输量以提高效率）。在 TCP/IP 体系中，该层功能被整合进了应用层。

![[Pasted image 20260727231630.png]]

### 应用层
应用层是网络协议栈的最顶层，直接为用户应用进程提供网络服务接口。应用层协议规定了应用程序之间通信的报文格式和交互规则。常见协议包括：HTTP/HTTPS（网页浏览）、FTP（文件传输）、SMTP/POP3/IMAP（电子邮件收发）、DNS（域名解析，将域名转换为 IP 地址）、DHCP（自动分配 IP 地址）、SNMP（网络管理）。应用层数据单元称为报文，通过 Socket 接口调用下层服务。
![[Pasted image 20260727231704.png]]
